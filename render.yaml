services:
  - type: web
    name: donation-pdf-filler
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: API_SECRET
        value: legendary2026
      - key: PYTHON_VERSION
        value: 3.11.0
